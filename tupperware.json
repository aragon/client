{
  "dependencies": {
    "phantomJs": false,
    "imageMagick": false
  },
  "preBuildCommands": [
    "sudo apt-get update && sudo apt-get install -y build-essential python", "npm install --global node-gyp; npm install --only dev", "npm install"
  ],
  "postBuildCommands": [],
  "buildOptions": {
    "mobileServerUrl": false,
    "additionalFlags": false
  }
}
